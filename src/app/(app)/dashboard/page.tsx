import { ModuleShell } from "@/shared/components/layout/ModuleShell";

export default function DashboardPage() {
  return (
    <ModuleShell
      title="Dashboard"
      description="Operations overview shell. KPIs and live metrics arrive in a later commit."
      emptyTitle="Dashboard shell ready"
    />
  );
}
